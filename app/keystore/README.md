# Environment signing keys

The active keystores live in the project root:

- `development_keystore` — `stagingRelease` and `uatRelease`
- `production_keystore` — `prodRelease`

Passwords and aliases are in the root `keystore.properties` file. Do not commit that file or the keystores.
